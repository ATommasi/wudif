import { Component } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'my-app',
    templateUrl: './shared/layout.html'

})
export class AppComponent {
  public title = 'WudIF Site';
}